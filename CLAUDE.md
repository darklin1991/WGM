# CLAUDE.md

本檔提供 Claude Code 在此 repo 工作時所需的專案知識。

## 專案概述

**WGM (Werewolf Game Master)** — Android 手機應用程式，狼人殺**法官（上帝）輔助工具**。

法官一人持有手機，負責開局配置、夜晚結算、白天流程控制、勝負判定與復盤。玩家仍以口頭語音進行遊戲，不使用 App。

不同的**板子**（角色配置）有不同的角色組合、夜晚行動順序與規則細節。App 不把板子寫死在程式碼裡，而是讀取**板子設定檔（JSON）**驅動整局流程。新增板子＝新增一份 JSON。

## 技術決策（已確認）

| 項目 | 決定 | 理由 |
|---|---|---|
| 框架 | Flutter (Dart) | 跨平台 UI 一致、法官操作介面客製化需求高 |
| 目標平台 | **僅 Android** | 可側載 APK，不必上架 |
| web/ 目錄 | **僅供開發預覽，不是交付目標** | `flutter run -d chrome` 熱重載幾秒就能看到 UI 改動，比等 Gradle build 五分鐘快得多。不要為 web 做任何相容處理或功能妥協 |
| 板子定義 | **設定檔驅動（JSON）** | 新增板子與規則變體不必改程式、不必重新發版 |
| 規則變體 | **寫在板子設定檔的 `rules` 旗標裡** | 不同賽制規則不同（同守同救、女巫自救…），由旗標控制 |
| 資料儲存 | **純單機本地** | 無後端、無連線、無多人同步 |
| 使用情境 | **只有法官操作 App** | 玩家不連線；不需帳號、不需即時通訊 |
| 版本控管 | **git ＋ GitHub remote（public）** | 擔當 2026-09-15 改為上傳 `https://github.com/darklin1991/WGM.git`（公開 repo）。提交前確認不含機敏資訊 

未經擔當同意不要引入：後端 API、連線多人、iOS 支援、玩家端介面。這些明確排除在現階段範圍外。

## 目前狀態

專案骨架、板子設定檔載入、夜晚結算引擎、夜晚流程 UI、完整白天流程（警長競選 → 警上發言 → 公布死訊 → 夜死開槍 → 警徽流 → 發言順序 → 逐人計時發言／騎士決鬥 → 放逐投票 → 警徽流）與勝負判定已完成，一整局可以從第一夜跑到分出勝負。已驗證：`flutter analyze` 無問題、662 項測試全數通過、release APK 建置成功。

內建 6 個板子（見 `assets/presets/`，全部 12 人），支援 18 種角色。**各板子的組成、特有規則與修改歷程見 `.claude/板子/`**（一個板子一份，檔名對應 presetId） —— 這裡只寫通則，板子個別的差異寫在那份。

尚未實作：本地持久化（App 關掉這一局就沒了）。**擔當於 2026-09-19 決定暫不做**，之後的 session 不必再提；要做由擔當主動提出。

## 開發環境（2026-09-16 於本機重建並驗證）

⚠️ 舊版本檔記載的 `D:\flutter`／`D:\Android\Sdk` 屬於**另一台機器**；本機 D: 是光碟機。工具鏈已改裝於 H:。

| 元件 | 版本 | 位置 |
|---|---|---|
| Flutter | 3.47.4 stable (Dart 3.13.3) | `H:\flutter` |
| JDK | Temurin 21.0.12.1 LTS（免安裝 zip，不需管理員權限） | `H:\jdk-21` |
| Android cmdline-tools | rev 23 | `H:\Android\Sdk\cmdline-tools\latest` |
| Android SDK | platform-tools 37.0.1、platforms/android-36、build-tools 36.0.0 | `H:\Android\Sdk` |
| NDK | 28.2.13676358 (r28c) | `H:\Android\Sdk\ndk` |
| Gradle | Flutter 自動取得 | `~\.gradle` |

**未安裝 Android Studio** —— 只靠 cmdline-tools 就能建置 APK，省下數 GB。若要用 AVD 模擬器才需要另外補裝。

Flutter 3.47.4 要求 compileSdk 36、minSdk 24、targetSdk 36、NDK 28.2.13676358（定義於 `H:\flutter\packages\flutter_tools\gradle\src\main\kotlin\FlutterExtension.kt`，換 Flutter 版本時查這個檔）。

已持久寫入使用者層級環境變數：`JAVA_HOME=H:\jdk-21`、`ANDROID_HOME`／`ANDROID_SDK_ROOT=H:\Android\Sdk`，PATH 追加 `H:\flutter\bin`、`H:\jdk-21\bin`、`H:\Android\Sdk\platform-tools`、`H:\Android\Sdk\cmdline-tools\latest\bin`。**要開新終端才生效**。

### 環境踩坑筆記（重裝或換機必讀）

網路上多數 Flutter 環境建置教學已經過時，以下是實際狀況：

**1. `sdkmanager` 已被 Google 棄用且會崩潰**

改用新的 `android` CLI，套件命名也從分號改成斜線：

```
H:\Android\Sdk\cmdline-tools\latest\bin\android.exe --sdk H:\Android\Sdk --no-metrics sdk list --all "ndk*"
H:\Android\Sdk\cmdline-tools\latest\bin\android.exe --sdk H:\Android\Sdk --no-metrics sdk install ndk/28.2.13676358
```

舊寫法 `platforms;android-36` → 新寫法 `platforms/android-36`。

**2. 新 CLI 安裝成功後仍回傳 crash 的 exit code**

安裝完成後工具自身會以 `-1073740791`（`0xC0000409`，STATUS_STACK_BUFFER_OVERRUN）結束。2026-09-16 重裝時四個套件全數如此，但檔案都正確落地。**不要用 exit code 判斷成敗，要驗證檔案是否存在**，例如 `H:\Android\Sdk\ndk\28.2.13676358\source.properties`。

**3. NDK 必須預先手動安裝**

Flutter 專案的 `ndkVersion = flutter.ndkVersion`（目前為 28.2.13676358）。若 NDK 不存在，Gradle 會去呼叫那個已壞掉的 `sdkmanager.bat`，build 直接失敗並吐出誤導訊息 `Package ndk not found`（其實遠端倉庫裡有）。**先用新 CLI 裝好 NDK**，Gradle 就不會去碰 sdkmanager。

其他套件（platform、build-tools）Gradle 走 AGP 內建下載機制，會自動補裝，不經 sdkmanager，沒有這個問題。

**4. Flutter 3.47.4 實際使用 API 36 / build-tools 36**

不是最新的 37.x。手動裝 37.x 沒有必要（裝了也無害，只是多佔空間）。

**5. JDK 用免安裝的 Temurin zip，不要用 winget**

`winget install Microsoft.OpenJDK.21` 會啟動 MSI 並跳出 UAC 管理員授權視窗，在非互動終端裡會直接卡死（`--silent --disable-interactivity` 也擋不住）。改抓 Temurin 的 zip 解開即可，不需要任何權限：

```
https://api.adoptium.net/v3/binary/latest/21/ga/windows/x64/jdk/hotspot/normal/eclipse
```

JDK 21 LTS 與 Gradle 9.x 相容良好。舊記錄用的 JDK 25 也能跑，但 21 是 AGP 官方支援範圍內的穩妥選擇。

**6. `flutter doctor` 只剩一條可忽略的警告**

- **Visual Studio not installed** —— 那是開發 **Windows 桌面** App 才需要的。本專案只做 Android，不必為它裝好幾 GB 的 C++ 工具鏈。
- 舊記錄提到的 **Android license status unknown** 這次沒有出現：新 `android` CLI 安裝套件時就寫入了授權檔，doctor 直接顯示 `All Android licenses accepted`。

**7. 第一次 `flutter build apk` 會很久**

實測約 **47 分鐘**（Gradle distribution 與全部相依都要下載）。不是卡住，不要中途砍掉。之後的增量 build 只要數十秒。建置時會出現 `SDK XML version 4` 警告，無害。

**8. PowerShell 5.1 解壓大型 zip**

`Expand-Archive` 很慢；用 `[System.IO.Compression.ZipFile]::ExtractToDirectory($zip, $dest)` 快得多。注意 5.1 沒有 `overwrite` 多載，第三個參數是 `entryNameEncoding`，傳 `$true` 會報型別轉換錯誤。

## 常用指令

```
flutter pub get                  # 安裝套件
flutter run                      # 在連線的 Android 裝置上除錯執行
flutter devices                  # 列出可用裝置
flutter analyze                  # 靜態分析（提交前必跑）
flutter test                     # 執行全部測試
flutter test test/xxx_test.dart  # 執行單一測試檔
dart format .                    # 格式化
flutter build apk --release      # 產生 release APK（側載用）
flutter clean                    # 清除建置產物
```

---

# 領域詞彙對照

程式碼用英文命名，但需求與討論用中文術語。對照如下，命名請一致：

| 中文 | 程式碼命名 | 說明 |
|---|---|---|
| 板子／配置 | `Preset` | 人數＋角色組合＋夜晚順序＋規則旗標 |
| 法官／上帝 | `moderator` | App 的唯一使用者 |
| 座次 | `seat` | 環狀，計算發言順序時需繞回 |
| 屠邊 | `sideElimination` | 神職全滅 **或** 平民全滅 → 狼勝 |
| 屠城 | `totalElimination` | 所有好人全滅 → 狼勝 |
| 刀 | `wolfKill` | 狼人擊殺 |
| 守 | `guardProtect` | 守衛守護 |
| 解藥／救 | `witchAntidote` | 女巫解藥 |
| 毒 | `witchPoison` | 女巫毒藥 |
| 查驗 | `seerInspect` | 預言家查驗 |
| 金水 | `verifiedGood` | 查驗結果為好人 |
| 查殺 | `verifiedWolf` | 查驗結果為狼人 |
| 奶穿／同守同救 | `guardHealConflict` | 同時被守與被救，依規則致死 |
| 開槍 | `hunterShot` | 獵人死亡技能 |
| 自爆 | `wolfSelfDetonate` | 狼人白天自爆，直接進夜 |
| 白痴翻牌 | `idiotReveal` | 被放逐時翻牌不死，失去投票權 |
| 魅惑 | `charm` | 狼美人指定殉情對象 |
| 殉情 | `loveSuicide` | 狼美人出局，被魅惑者隨之死亡 |
| 決鬥 | `knightDuel` | 騎士白天翻牌與一人對決 |
| 學習 | `mechanicLearn` | 機械狼複製一名玩家的身分技能 |
| 帶刀 | `carriesKnife` | 機械狼在小狼全滅後才取得狼刀 |
| 破盾 | `shieldBroken` | 雙刀集中同一人，守衛與解藥都失效 |
| 毒反彈 | `poisonReflected` | 機械狼的守護把毒反噬給下毒者 |
| 槍牌 | `gunRole` | 死亡可開槍的身分（獵人、狼王） |
| 吃刀／吃毒／吃推 | `wolfKill` / `poison` / `exile` | 被狼刀／被毒／被投票放逐 |
| 警長 | `sheriff` | 票權加權 |
| 上警 | `runForSheriff` | 參選 |
| 退水 | `withdrawFromElection` | 退出競選 |
| 警上發言 | `campaignSpeech` | 競選發言 |
| 警徽流 | `badgeSuccession` | 警長出局後把警徽交給誰（或撕毀） |
| 警左／警右 | `fromSheriffLeft` / `fromSheriffRight` | 發言起點 |
| 死左／死右 | `fromDeceasedLeft` / `fromDeceasedRight` | 發言起點 |
| 平票 PK | `tieBreakRound` | 平票者再發言後重投 |
| 放逐 | `exile` | 投票出局 |
| 禁言 | `silenced` | 該輪不可發言 |
| 復盤 | `review` | 賽後檢討 |

---

# 功能模組

## 1. Game Setup & Registry（房間與配置管理）

- **板子配置 (Preset Builder)**：自訂人數與身分組合（如預女獵白、狼王守衛局）。可從內建範本建立、修改後另存。
- **玩家管理 (Player Registry)**：編號、座次、生死狀態、狀態標記。

狀態標記分兩類，**不要混在一起**：

- **事實標記**（引擎結算用）：`guarded`、`knifed`、`healed`、`poisoned`、`shot`、`exiled`
- **資訊標記**（法官備忘與復盤用，不影響結算）：`verifiedGood`（金水）、`verifiedWolf`（查殺）、`silenced`（禁言）

## 2. Night Phase Arbitrator（夜晚結算器）

- **行動佇列 (Action Queue)**：依角色優先級排序技能釋放順序。順序來自板子設定檔的 `nightOrder`。
- **衝突裁決 (Conflict Resolution)**：同守同救（奶穿）、自爆與鎖定判定。

**關鍵：收集與結算必須分開兩階段。**

1. **收集階段**：法官依 `nightOrder` 逐一輸入各角色行動，只記錄意圖，不做判定。
2. **結算階段**：全部收集完後一次性套用衝突規則，產出當夜死亡名單。

### 身分一律在開局前登記完，夜裡不再問座次

**擔當於 2026-09-19 決定：維持「進入第一夜」必須先配滿全部身分**
（`player_registry_page` 的 `assignmentMatchesPreset`），不開放未配身分就開局。

連帶兩條，**不要當成 bug 改回去**：

- **首夜不再逐一問「你是幾號」** —— 身分已經在 `GameState` 裡了。
  引擎用 `NightFlowMachine.needsRegistration()` 判斷，該步的角色若都已有座次
  就跳過登記子階段，**角色照樣喊、技能照樣收**。
- **確認過身分的角色，夜裡不再叫起來**（擔當 2026-09-19 指定）。
  白痴、騎士這類**沒有夜間技能**的角色，那一步本來只是為了問出座次，
  座次已知就整步跳過（`_isNoOpStep`）。這不會洩漏資訊 —— 第二夜起本來
  就不會再叫他們，每一夜的流程長度反而一致。

引擎仍保留「夜裡邊問邊登記」那條路（`registerSeats`／`pickSpecial`／
結算時補平民），單元測試也還在跑它，但**介面目前不提供入口**。
要開放由擔當主動提出。

### 查驗結果必須當場給，不能只留在結算頁

預言家／通靈師選完目標後，流程會停在 `NightSub.inspectResult`，
由法官把**金水／查殺**（通靈師則是真實身分）比給查驗者看，按下一步他才閉眼。

**這一步不能省。** 夜晚結算頁固然也會列出查驗結果，但那是整夜跑完之後的事，
那時查驗者早就閉眼，法官已經沒機會告知。這一步之於預言家，
等同開槍手勢之於獵人。

引擎只回傳 `InspectReveal`（座次、是否通靈師、角色、是否機械狼），
「金水」「查殺」怎麼寫是 UI 的事。空驗（沒選人）不會停。

**機械狼的資訊時序也是特例**。牠不與小狼相認，所以有兩件事只有法官知道、必須用手勢告訴牠：今晚有沒有刀，以及學到了什麼身分／能不能開槍。這兩件事的時機不同，所以機械狼一晚要睜眼**兩次**：

- **開頭那一輪**（`NightSkill.mechanicTurn`，**每晚都有**）：先給**開刀手勢**（小狼是否已全滅 → 今晚有沒有刀）→ 有刀就選**第一刀** → 是否使用技能。就算沒技能可用（學到平民、或學了還沒生效）也要叫起來，否則機械狼無從得知自己有沒有刀。要收的技能記在 `NightStep.mechanicSubSkill`，不是 `skill`。
- **結尾那一輪**（`NightSkill.mechanicReveal`）：告知學到的身分與開槍手勢。非得排在最後不可 —— 牠第一個睜眼時身分都還沒登記完、女巫的毒也還沒收，那時法官根本講不出來。

不要為了少一個步驟就把結尾那輪併回開頭，也不要因為「這晚沒技能」就省掉開頭那輪。

有覺醒石像鬼的板子，首夜在機械狼結尾那一輪**之後**還有一步：逐位叫醒被轉換者告知（`NightSkill.convertedNotify`）。兩隻石像鬼一定各轉換一位、不會撞車（擔當 2026-09-25 指定），所以固定叫兩次；某隻鄰座全是狼隊、沒得轉換時那一格沒有人，照樣喊。放在機械狼之後不違反牠「排最後」的理由（要等身分登記完、毒收完），詳見 `.claude/板子/12p_fengsheng_dieying.md`。

**第二夜起，狼隊之前還有轉換者的開刀手勢**（`NightSkill.convertedTurn`，擔當 2026-09-25 指定）。道理同機械狼：被轉換者不與任何人相認，不知道石像鬼與機械狼死光了沒，有沒有刀只有法官知道。一樣逐位叫、固定叫石像鬼的數量；有刀的那一位就在這一輪選刀口，狼隊那一格改走過場。位置是一般板子守衛的位置 —— 風聲諜影沒有守衛，所以是攝夢人之後、石像鬼之前。

小狼全滅後，狼刀由機械狼在自己那一輪開，不需要另外生一個「機械狼帶刀」的步驟。

### 角色全滅的步驟一律走過場，不要跳過

法官若因為某個身分死光就不喊它，玩家馬上從流程長度聽出誰出局了。所以**第二夜起沒有任何步驟會被跳過** —— 角色全滅的步驟改為走過場（`NightSub.passThrough`）：照常喊、照常停頓，只是沒有東西要收。喊的是角色名（`primaryRole.nameZh`）而不是步驟標題，否則「機械狼（守衛）請睜眼」會直接露餡。

狼隊那一步是唯一的例外組合：機械狼學到狼人時，那一格就是牠的**第二刀**（第一刀在開頭那一輪砍）。兩刀分開在第 1 格與第 3 格收，也剛好讓同一人可以被選兩次（破盾）。

**查驗真實身分的角色要排在夜晚順序的後段**。通靈師查的是真實身分，法官必須先登記完所有人才答得出來 —— 所以 12人機械狼通靈師板把它排在最後（`nightOrder` 的 `psychic`）。獵人夾在女巫與通靈師之間：獵人要排在女巫之後（先收完毒才知道給哪個開槍手勢），排在通靈師之前則是為了讓通靈師殿後。

**女巫的資訊時序是特例**（也是奶穿的成因）：女巫看到的是「**狼刀目標**」，**不套用守衛結果**。因此女巫可能對一個已被守衛守住的人用解藥，最終才在結算階段判定為同守同救。實作時不要為了方便就先把守衛結果算進去給女巫看 —— 那會讓奶穿永遠不可能發生。

**結算完就是白天。** `NightFlowMachine._finish()` 在 `apply()` 之後把 `GameState.phase` 設成 `day`（進夜時由 `NightFlowPage.enterNight` 設成 `night`）。白貓靠它分辨夜裡判死（當天投票後離場）與白天判死（隔天才離場）—— 2026-09-24 之前沒有任何地方把它設成 `day`，白天判死一律被當成夜裡。順序不能對調：夜裡判死的白貓要在 `night` 階段延後。

## 3. Day Phase Controller（白天流程控制）

- **警長競選 (Sheriff Election)**：已實作於 `day_controller.dart`，見下方「警長競選」。
- **發言與計時 (Turn & Timer)**：順序見下方「發言順序」，計時見下方「發言計時」。
- **騎士決鬥 (Knight Duel)**：見下方「騎士決鬥」。
- **投票管理 (Voting System)**：投票紀錄、平票 PK、放逐結算。
- **警徽流 (Badge Succession)**：警長出局後的移交或撕毀，見下方「警徽流」。

### 警長競選

**排在第一天公布死訊之前。** 昨晚死的人這時還沒被公布，照樣可以上警、可以投票 —— 所以流程是「夜晚結算 → 警長競選 → 公布死訊」，不是先講誰死了再選。`sheriffElection` 旗標關掉就跳過整個競選（無警長局）。

階段：**上警 → 警上發言 → 退水 → 投票 →（平票時 PK 重投）**。警上發言逐人計時，見下方「發言計時」。

投票採**按候選人歸票**：法官喊「投 N 號的請舉手」，先點候選人再圈選舉手的人 —— 這是現場真正的做法，比逐一問每個人快得多。四個防呆：

- **一人一票**：投過的人換候選人時點不動，顯示「已投 N 號」
- **候選人與退水者從頭就不能投**（擔當選的賽制：兩者都不投）
- **未投票名單列出來**，法官不會漏人；沒圈到的就是棄票
- **票數即時算**

判定：

| 情況 | 結果 |
|---|---|
| 沒人上警 | 無警長，直接跳過 |
| 全部退水 | 無警長 |
| 只剩一位候選人 | 直接當選，不必投票 |
| 最高票單一 | 當選 |
| 全員棄票 | 無警長 |
| 平票 | 平票者 PK 重投，票數歸零 |
| PK 後仍平票 | 本局無警長 |

**PK 輪的投票權**：只有 PK 名單裡的不投；第一輪落選的其他候選人**恢復投票權**（擔當選的規則）。退水的人是自己退出整場競選，PK 輪仍然不投。

競選時還沒有警長，所以算票**不套用** `sheriffVoteWeight`。

### 警徽流

警長出局後由警長本人決定警徽去向，實作在 `badge_succession.dart`。
擔當於 2026-09-19 指定的賽制：

| 項目 | 規則 |
|---|---|
| 移交對象 | **任一存活玩家** —— 不必事先宣告名單，警長口頭指定，法官當場點下去 |
| 撕警徽 | **可以**，此後本局無警長 |
| 警長被毒死 | **照常可以移交** —— 毒藥只擋獵人開槍，不擋警徽 |

因為不必事先宣告名單，**沒有「宣告警徽流」這個步驟**，不要另外加一個。

**觸發條件只有一條：有警長，而且那位已經出局**（`BadgeSuccession.isDue`）。
刻意不看「是不是這一輪死的」—— 第一天的競選排在公布死訊之前，昨晚死掉的人
照樣可以上警當選，那種情況一公布死訊就要立刻移交。

**插在流程的哪裡**（`BadgeSuccessionPage.routeIfDue` 夾在兩個環節中間）：

| 警長怎麼死的 | 警徽流的位置 |
|---|---|
| 夜裡出局 | 公布死訊與夜死開槍**之後**、決定發言順序**之前** |
| 放逐／開槍／殉情 | 放逐結算**之後**、進下一夜**之前** |

夜死那條非得排在發言順序前面不可 —— 警左警右由警長決定，得先知道現在誰是警長。
撕掉警徽的話 `sheriffSeat` 變 null，發言順序就自動改走「沒有警長」的隨機規則。

移交不改動 `GameState` 的任何新欄位，只改 `sheriffSeat`，所以
`copy()`／`restoreFrom()` 不必跟著動。

### 夜死開槍

夜裡出局的槍牌（獵人、被自刀的狼王、學到槍牌且吃刀的機械狼）天亮後開槍，
實作在 `night_death_shot.dart`（純 Dart）與 `night_death_shot_page.dart`。
誰有資格由夜晚結算決定，記在 `NightOutcome.shooterSeats` ——
`hunterMayShoot` 是獵人本人與機械狼共用的，光看它分不出是誰開槍。

**位置（擔當 2026-09-24 指定）：公布死訊之後、警徽流之前。**

```
（第一天）警長競選 → 公布死訊 → 夜死開槍 → 警徽流 → 發言順序
```

排在警徽流前面，是因為獵人可能帶走的就是警長 —— 得先開完槍才知道警徽要不要移交。
`NightDeathShotPage.routeIfDue` 的 `next` 是延遲求值的，所以警徽流的 `isDue`
是開完槍才判斷。沒有人能開槍就不插這一頁。

| 項目 | 規則 |
|---|---|
| 連鎖 | 與放逐時的開槍相同：目標死亡 → 狼美人殉情。被帶走的人**不再開槍** |
| 放棄開槍 | 要明確按「放棄開槍」，照樣記進日誌 |
| 帶走白貓 | 已經是白天，延到**隔天**的放逐投票才離場。**延後中**的白貓不能選（擔當 2026-09-24 指定） |
| 發言順序 | 「單死／雙死」**連天亮後被槍帶走的人也算**（擔當 2026-09-24 指定）—— 獵人夜死又帶走一人就是雙死，從警長算起 |
| 宣布稿 | 不寫開槍者的身分 —— 機械狼學到獵人時，寫出來就露餡 |

開完槍立刻檢查勝負，帶走最後一匹狼就直接進結果頁。

### 騎士決鬥

實作在 `knight_duel.dart`（純 Dart）。

**時機**：警長競選結束之後、放逐投票開始之前 —— 也就是白天的發言階段。
上警階段不可決鬥，投票開始後也不可。入口是**發言計時頁上的一顆按鈕**，
整個發言階段都開著；一離開那一頁（進投票）視窗就關了。決鬥視窗就是這顆按鈕，
不要再散到別處判斷。

| 項目 | 規則 | 旗標 |
|---|---|---|
| 對手是狼人 | 對手死亡，**立即進入黑夜**，跳過當天投票 | `knightDuelEndsDay` |
| 對手是好人 | **騎士自己出局**，白天照常走到投票 | — |
| 決鬥次數 | **整局只有一次**，決鬥到狼活下來也不能再發動 | — |
| 對手是槍牌 | **不能開槍** —— 決鬥是騎士的裁決，對手沒有反擊機會 | — |
| 對手是狼美人 | 被魅惑者**不殉情** | `knightDuelBlocksCharmSuicide` |
| 對手是延後中的白貓 | **不能選**（擔當 2026-09-24 指定）—— 打中的話他會當場死亡、跳過延後 | — |

後三條是擔當 2026-09-19 指定的。死因一律是 `DeathCause.knightDuel`
—— 刻意不是狼刀也不是放逐，所以不會被當成「可以開槍的死法」。

狼美人那一條是**騎士唯一能救下殉情者的時機**，夜晚結算碰不到。

「整局一次」記在 `GameState.knightDuelUsed`，所以 `copy()` 與
`restoreFrom()` 都有它 —— 撤銷決鬥時技能要跟著還回來。

**決鬥出狼走的是和自爆相同的「立即進夜」路徑**，但**警徽流照樣要走**
—— 被決鬥掉的可能就是警長。死完也要重新檢查勝負（可能決鬥掉最後一匹狼）。

## 4. Settlement & Win Check（勝負與結算引擎）

判定在 `win_checker.dart`（`WinChecker.check(state) → WinCheck`），純 Dart。
**目前內建的六個板子都是屠邊。**

| 條件 | 結果 | 適用賽制 |
|---|---|---|
| 狼人全滅 | 好人勝 | 兩種都是 |
| 神職全滅 **或** 平民全滅 | 狼人勝 | `sideElimination`（屠邊） |
| 所有好人全滅 | 狼人勝 | `totalElimination`（屠城） |

三條規矩：

1. **狼全滅優先判定。** 同一次結算裡狼與神職一起死光，是好人勝 —— 狼都沒了就沒有人能贏下去。
2. **板子裡沒配置的陣營不算「全滅」。** 無平民的板子，`aliveVillagerCount` 開局就是 0，不先擋掉會直接判狼勝。所以要先看 `preset.villagerCount`／`preset.godCount` 有沒有配置。
3. **白痴翻牌後還活著就算神職存活。** 判定看 `alive`，不看 `canVote`。

**每次死亡事件後都要重新檢查，不是只在白天結束時。** 目前的呼叫點：

- `night_result_page.dart` —— 夜晚結算完，分出勝負就不進白天。
- `exile_vote_page.dart` —— 放逐、開槍、殉情整串連鎖跑完後檢查一次。連鎖中途不必逐步檢查，反正連鎖不會因為中途贏了就停下來。
  **會推進流程的按鈕都要走 `_advance()`** —— 河豚的「翻牌帶走」以前直接呼叫引擎，結算完漏掉勝負檢查。
- `night_death_shot_page.dart` —— 每開一槍檢查一次。

分出勝負一律 `pushReplacement` 進 `GameOverPage`（宣布結果＋全員身分揭曉）。

復盤日誌見下方「復盤日誌」一節。

---

# 架構

```
lib/
  main.dart
  core/
    models/
      role.dart            # 角色定義（陣營、夜晚行動、優先級）
                           # 寫死在程式碼的 const，不是設定檔 ——
                           # 設定檔驅動的是**板子**，不是角色
      preset.dart          # 板子配置＋規則旗標
      player.dart          # 玩家（編號、座次、生死、狀態標記）
      game_state.dart      # 完整局面狀態（可變，就地修改）
      night_action.dart    # 夜間行動意圖
      log_entry.dart       # 復盤日誌項目（只寫入，不用於推導狀態）
      preset_format_exception.dart # 板子設定檔驗證失敗（指出哪個板子、哪個欄位）
    engine/
      night_arbitrator.dart  # 夜晚結算：行動佇列＋衝突裁決
      night_flow.dart        # 依板子產生夜晚步驟序列
      night_flow_machine.dart # 夜晚流程狀態機（純 Dart，規則不在頁面裡）
      day_controller.dart    # 白天流程狀態機
      vote_resolver.dart     # 投票／平票 PK／放逐
      badge_succession.dart  # 警徽流：警長出局後的移交／撕毀
      night_death_shot.dart  # 夜死開槍：夜裡出局的槍牌天亮後開槍
      seat_block_reason.dart # 禁選原因列舉（夜晚白天共用）＋白天共用的禁選
      knight_duel.dart       # 騎士翻牌決鬥
      win_checker.dart       # 屠邊／屠城判定
      speech_order.dart      # 發言順序計算
      speech_timer.dart      # 發言計時（純邏輯，碼表由頁面驅動）
      seat_ring.dart         # 環狀座次走訪（跳過死亡玩家）
      undo_stack.dart        # 撤銷快照堆疊（局面＋流程位置）
    rules/
      rule_flags.dart        # 規則旗標解析與預設值
    log/
      game_log.dart          # 復盤日誌：分段、匯出純文字
    storage/
      preset_loader.dart     # 讀 assets/presets/ 的板子 JSON 並驗證
                             # （core 裡唯一碰 Flutter 的檔 —— rootBundle）
                             # 本地持久化尚未實作，擔當 2026-09-19 決定暫不做
  features/
    setup/     # 板子配置＋玩家登記
    night/     # 夜晚結算介面
    day/       # 白天流程（競選、發言計時、投票）
    review/    # 復盤日誌、勝負結果（回放尚未做）
  shared/      # 共用 widget、主題、工具
assets/
  presets/     # 內建板子 JSON（pubspec 只宣告這一個目錄）
test/
```

## 關鍵設計原則

**0. 流程狀態機必須在引擎裡，不能寫在頁面裡**

「哪一步接哪一步、這步能選誰、目標記到哪個欄位」全是**規則**，不是排版。
夜晚流程在 `night_flow_machine.dart`（純 Dart），頁面只負責把狀態畫出來、
把操作轉回去。白天流程照同一個模式做 —— 否則 `day_controller` 會重蹈覆轍，
長成另一個上千行、只能靠 widget 測試驗規則的檔案。

連帶兩條：

- **禁選原因用 `SeatBlockReason` 之類的列舉回傳，不要回中文字串** ——
  判斷在引擎、措辭在 UI。列舉在 `seat_block_reason.dart`，夜晚與白天共用；
  延後中的白貓夜晚白天都用 `pendingCatBlocks`，白天開槍與決鬥共用的限制是
  `dayBlockedSeats`，措辭統一走
  `seat_picker.dart` 的 `seatBlockReasonZh`。
- **頁面不得在 `initState` 改 `GameState`。** 推進夜次由 `NightFlowPage.route`
  （或 `enterNight`）在 push 之前做，撤銷快照才有地方存；否則法官進錯頁面
  退出來，狀態就已經髒了。

**1. 狀態直接修改（擔當已決定）**

`GameState` 為可變狀態，引擎直接就地修改，不採事件溯源。這條路實作直觀、上手快，但**復盤與撤銷必須另外處理**，不會自動附帶：

- **復盤／回放**：已實作於 `game_log.dart`，見下方「復盤日誌」。日誌是**單向的** —— 只寫入、供事後查看與匯出，**不用來推導狀態**。務必在修改狀態的同一處就寫日誌，否則很容易漏記，事後對不上。回放（把局面倒回某一步）尚未做。
- **撤銷（undo）**：法官現場誤觸是常態，已實作於 `undo_stack.dart`。夜晚流程每按一次「下一步」存一筆，顆粒度到單一夜晚行動；白天的每次投票也要照做。
  - 快照存的是**動作發生之前**的局面，所以撤銷取出來的就是上一步。
  - **只還原 `GameState` 不夠**：局面回去了但流程還停在後面一步，法官會看到對不上的畫面。所以快照另帶一個 `cursor`（夜晚是 `NightCursor`：走到第幾步、哪個子階段、已收到哪些行動意圖），`UndoStack` 不解讀它，原樣存原樣還。
  - 還原用 `GameState.restoreFrom` **就地覆寫**，不換物件 —— 頁面與狀態機到處持有同一個參照，換物件就得全部重接。
  - `GameState`／`Player` **新增欄位時，`copy()` 與 `restoreFrom()` 都要同步改**，漏一個撤銷就會留下殘影。`test/core/undo_test.dart` 有一項測試逐欄位檢查。

**2. 引擎必須是純 Dart，不依賴 Flutter**

`core/engine` 與 `core/rules` 不得 import 任何 widget。夜晚結算與勝負判定的規則組合極多（衝突規則 × 規則旗標），必須能用純單元測試涵蓋。**衝突裁決表的每一列都應該有對應測試。**

**3. 規則旗標集中在一處**

不要讓「同守同救是否致死」這類判斷散落在各處。所有規則變體集中由 `rule_flags.dart` 提供，引擎讀旗標決策。

**4. 角色可擴充**

新增角色（狼王、狼美人、守衛、騎士…）應只需新增角色定義與其行動處理，不改動結算主流程。

---

# 板子設定檔格式

`assets/presets/<preset_id>.json`：

```json
{
  "presetId": "12p_yu_nu_lie_bai",
  "name": "12人 預女獵白",
  "playerCount": 12,
  "roles": [
    { "role": "wolf",     "count": 4 },
    { "role": "seer",     "count": 1 },
    { "role": "witch",    "count": 1 },
    { "role": "hunter",   "count": 1 },
    { "role": "idiot",    "count": 1 },
    { "role": "villager", "count": 4 }
  ],
  "nightOrder": ["wolf", "witch", "seer"],
  "rules": {
    "guardHealKills": true,
    "witchSelfHealNight": 1,
    "witchDualUseSameNight": false,
    "guardCannotRepeatTarget": true,
    "poisonedHunterCannotShoot": true,
    "sheriffVoteWeight": 1.5,
    "tieBreak": "pk_then_none",
    "winCondition": "sideElimination",
    "wolfSelfDetonateEndsDay": true
  }
}
```

載入時**必須驗證**：`roles` 的 count 總和等於 `playerCount`；`nightOrder` 內的角色都存在於 `roles`；旗標值在合法範圍。驗證失敗要指出是哪個板子、哪個欄位錯，不要讓錯誤的設定檔在結算到一半才炸掉。

## 規則旗標

| 旗標 | 含義 |
|---|---|
| `guardHealKills` | 同守同救是否致死（奶穿）。`true` 為多數賽制 |
| `witchSelfHealNight` | 女巫可自救的夜次：`0` 不可、`1` 僅首夜、`-1` 不限。**未指定時依人數推導**，見下方說明 |
| `witchDualUseSameNight` | 同一夜是否可同時用解藥與毒藥 |
| `guardCannotRepeatTarget` | 守衛不可連續兩晚守同一人（應在輸入階段就擋住） |
| `poisonedHunterCannotShoot` | 獵人被毒死是否不可開槍 |
| `sheriffVoteWeight` | 警長票權重（通常 1.5） |
| `tieBreak` | 平票處理：`pk_then_none`／`pk_then_revote`／`none` |
| `winCondition` | `sideElimination`（屠邊）／`totalElimination`（屠城） |
| `wolfSelfDetonateEndsDay` | 自爆是否立即結束白天、跳過投票進入夜晚 |
| `charmCannotRepeatTarget` | 狼美人不可連續兩晚魅惑同一人（應在輸入階段就擋住） |
| `wolfBeautyCannotSelfKill` | 狼隊不可把刀指向狼美人自己 |
| `knightDuelBlocksCharmSuicide` | 狼美人被騎士決鬥致死時，殉情不發動 |
| `knightDuelEndsDay` | 騎士決鬥到狼後，是否立即結束白天進入黑夜 |
| `mechanicGuardReflectsPoison` | 機械狼（學到守衛）的守護是否把毒反彈給下毒者 |
| `mechanicDoubleKnifeBreaksShield` | 機械狼（學到狼人）雙刀集中同一人是否破盾 |
| `sheriffElection` | 本局是否有警長競選（警長局）。關掉就跳過整個競選 |
| `speechSeconds` | 每人發言額度（秒），預設 120。法官在計時頁仍可現場改 |

### 依人數決定的規則

**9 人以上，女巫不可自救**（8 人以下的小局僅首夜可自救）。

這是依人數決定的通則，所以做成 `RuleFlags.defaultWitchSelfHealNight(playerCount)` 推導，**不寫死在每份設定檔裡** —— 新增板子時不必記得填 `witchSelfHealNight`，規則自動生效。個別板子若採用不同賽制，仍可在 `rules.witchSelfHealNight` 明確覆寫。

`test/core/preset_assets_test.dart` 會直接讀 `assets/presets/` 的實際檔案驗證這條規則，內建板子若違反會在測試階段就被抓到，而不是等法官開場才發現。

---

# 衝突裁決表

夜晚結算的核心規則。**每一列都要有對應的單元測試。**

| 情況 | 結果 | 控制旗標 |
|---|---|---|
| 狼刀 ＋ 守衛守（同一目標） | 存活 | — |
| 狼刀 ＋ 解藥（同一目標） | 存活 | — |
| 狼刀 ＋ 守衛守 ＋ 解藥（同守同救／奶穿） | **死亡** | `guardHealKills` |
| 毒 ＋ 守衛守（同一目標） | **死亡**（守衛防不了毒） | — |
| 狼刀 ＋ 毒（同一目標） | 死亡 | — |
| 守衛連續兩晚守同一人 | 輸入階段擋住 | `guardCannotRepeatTarget` |
| 獵人被毒死 | **不可**開槍 | `poisonedHunterCannotShoot` |
| 獵人被狼刀死 | 可開槍 | — |
| 獵人的開槍手勢（夜裡預告） | 用當下收到的行動**試算一次結算**：會死而且死法不能開槍（毒、夢死…）才比「不可開槍」；今晚活得下來（例如被毒但正在夢遊）就是槍完好。手勢與天亮後的結算永遠一致（擔當 2026-09-25 指定要考慮夢死） | — |
| 獵人被放逐 | 可開槍 | — |
| 白痴被放逐 | 翻牌不死，失去投票權，留在場上 | — |
| 白痴被狼刀／被毒 | 正常死亡 | — |
| 狼美人夜裡出局（刀／毒／奶穿） | 被魅惑者**殉情** | — |
| 狼美人本晚沒魅惑 | **沒選就是沒魅惑** —— 前一晚的對象一併解除，不沿用 | — |
| 狼美人被騎士決鬥致死 | 被魅惑者**不殉情** | `knightDuelBlocksCharmSuicide` |
| 騎士決鬥致死的槍牌 | **不可**開槍（死因不是刀也不是推） | — |
| 騎士決鬥過一次 | 整局不能再發動，活下來也一樣 | — |
| 被魅惑者已因他故死亡 | 死因維持原本的，不改記殉情 | — |
| 被魅惑者被守衛守住，但狼美人死了 | **死亡**（守衛防不了殉情） | — |
| 殉情死的獵人 | 可開槍（死因不是毒） | — |
| 狼美人連續兩晚魅惑同一人 | 輸入階段擋住 | `charmCannotRepeatTarget` |
| 狼刀指向狼美人 | 輸入階段擋住 | `wolfBeautyCannotSelfKill` |
| 機械狼與原守衛各守一人 | 兩人都擋得住，紀錄各自獨立 | — |
| 機械狼（學到守衛）守 ＋ 女巫救 | **死亡**（同守同救照樣成立） | `guardHealKills` |
| 機械狼（學到守衛）守護的人被毒 | 目標存活，**毒反彈毒死下毒者** | `mechanicGuardReflectsPoison` |
| 機械狼（學到女巫）的藥 | **只有一瓶毒藥，沒有解藥** | — |
| 小狼還活著時的機械狼 | **沒有刀**，學到狼人也砍不了 | — |
| 機械狼帶刀（小狼全滅）＋學到狼人 | 該晚砍兩刀 | — |
| 雙刀砍不同人 | 兩刀各自結算 | — |
| 雙刀砍同一人 | **破盾**，守衛的守護與女巫的解藥都失效，必死 | `mechanicDoubleKnifeBreaksShield` |
| 破盾 ＋ 守 ＋ 解藥 | **死亡**，死因是狼刀（守與救都被打穿，**不構成奶穿**） | — |
| 機械狼（學到槍牌）吃刀／吃推出局 | 可開槍 | — |
| 機械狼學到槍牌 | **學到就有槍** —— 學習當晚被刀、隔天白天被放逐都能開，不受技能隔夜生效限制（擔當 2026-09-24 指定） | — |
| 機械狼學到河豚、白貓 | 同槍牌**學到就生效**（被動技能）；學到熊、攝夢人照一般規則隔夜生效。細節見 `.claude/板子/12p_fengsheng_dieying.md` | — |
| 機械狼學到暗戀者、覺醒石像鬼 | 只套用身分偽裝，**沒有技能** | — |
| 機械狼（學到槍牌）吃毒或殉情出局 | **不可**開槍（比獵人本人嚴格） | — |
| 機械狼學習當晚 | 技能**不生效**，隔夜才生效（**槍除外**，學到就有） | — |
| 機械狼學過之後被查驗 | 查到的是**牠學到的身分**，不是機械狼 | — |
| 上述偽裝的生效時機 | **學習當晚就生效** —— 與技能的隔夜生效刻意不同步 | — |
| 預言家查學到好人身分的機械狼 | **金水** —— 偽裝連預言家也騙過 | — |
| 機械狼學到狼人後被查驗 | 顯示狼人，預言家仍是查殺 | — |
| 機械狼還沒學習時被查驗 | 照實顯示機械狼（預言家查殺） | — |
| 機械狼夜晚睜眼 | **兩次**：開頭給開刀手勢並行動，結尾聽身分與開槍手勢 | — |
| 機械狼開頭那一輪 | **每晚都有**，就算沒技能可用也要給開刀手勢 | — |
| 機械狼（學到狼人）的兩刀 | 第一刀在開頭那一輪，第二刀在狼人那一格 | — |
| 機械狼學習對象 | 不能選自己 | — |
| 機械狼學到平民／白痴／騎士 | 無夜間技能，之後不再叫起來 | — |
| 任一身分全滅 | 該步驟保留為走過場，照常喊、照常停頓，不跳過 | — |
| 覺醒石像鬼首夜轉換 | **一定各轉換一位、不會撞車**：範圍是**兩隻石像鬼的左右鄰座合起來**，第二隻選不到第一隻選過的人；石像鬼不能選石像鬼，**機械狼不擋**（互不相認，擋了會洩漏），選到的話轉換悄悄白費；範圍裡沒得選時才放行（擔當 2026-09-25 指定） | — |
| 被轉換者持刀（開刀順位第 3 順位） | 在第二夜起狼隊之前的**開刀手勢**那一輪開刀，狼隊那一格走過場 | — |
| 被轉換者接刀的那一夜 | 原技能**當夜就失效**，那一步改走過場（人會睜眼，但沒有東西要收） | — |
| 已接刀的被轉換獵人出局（刀、毒、推） | **不可**開槍 | — |
| 延後中的白貓 | 刀、毒、救、守、攝夢、開槍、騎士決鬥**都不能選**，擋在輸入階段 | — |

## 發言順序

已實作於 `speech_order.dart` 與 `seat_ring.dart`。座次是**環狀**的，走訪時要跳過死亡玩家並繞回。

決定起點的規則（擔當指定）：

| 情況 | 起點 | 方向 |
|---|---|---|
| **單死** | 死者（桌上說的「死左／死右」） | 警長選 |
| **平安夜、雙死以上** | 警長（「警左／警右」） | 警長選 |
| **沒有警長** | 隨機抽一位存活玩家 | **也隨機** |

沒有警長時整組都隨機 —— 沒人有權決定方向，就由上帝抽。這條**優先於單死**：就算只死一個，沒警長也走隨機，因為死左死右的方向本來就該由警長決定。

從起點往選定方向的**第一位存活玩家**開始繞一圈：

- 起點是**警長**（還活著）→ 警長排在**最後**發言
- 起點是**死者** → 死者不在名單裡
- 起點是**隨機抽到的人** → 他自己**先**發言

**「單死／雙死」的死者怎麼算**（擔當 2026-09-24、09-25 指定）：昨晚的死者，
**加上天亮後被槍帶走的人**；**延後離場的白貓也算**。白貓還活著，所以他若是
起點（單死），會像警長一樣排在最後發言。

### 方向用順時鐘／逆時鐘表示，不要用「左／右」

- 順時鐘 = 座號**遞增**（5 → 6 → 7…）
- 逆時鐘 = 座號**遞減**（5 → 4 → 3…）

桌上說的「警左／警右」對應哪個方向，各家習慣不同 —— **App 不自己定義**。畫面把兩個方向的實際號碼順序都列出來，法官照警長講的挑，挑錯一眼就看得出來。

警長出局後的移交見「警徽流」一節。

## 發言計時

碼表邏輯在 `speech_timer.dart`（純 Dart），畫面在 `speech_timer_view.dart`。
擔當於 2026-09-19 指定的賽制：

| 項目 | 規則 |
|---|---|
| 每人額度 | 板子旗標 `speechSeconds`，預設 **120 秒**，**法官現場可調**（畫面上有 1/1.5/2/3 分的快選） |
| 時間到 | **只提示，不自動跳下一位** —— 變紅、震動一次，繼續往下數成超時。什麼時候換人由法官決定 |
| 計時範圍 | 警上發言、白天發言、平票 PK 發言、遺言 **四種都計** |

**`SpeechTimer` 不自己跑碼表** —— 每秒呼叫一次 `tick()` 是頁面的事。這樣
「剩幾秒、超時多久、能不能往回退」全部可以用同步測試驗，不必碰假 async。
計時不動 `GameState`，所以不需要撤銷快照，按錯直接 `previous()` 或 `jumpTo()`。

四個使用點的接法不同：

| 環節 | 怎麼接 | 發言順序哪裡來 |
|---|---|---|
| 警上發言 | `ElectionStage.campaignSpeech`，碼表**嵌在競選頁** | `SheriffElection.campaignSpeechOrder`（上警者，座號遞增） |
| 白天發言 | 獨立的 `SpeechTimerPage`，夾在發言順序頁與放逐頁中間 | `SpeechOrderPage.onFinished` 把確定的順序傳出來 |
| 平票 PK 發言 | `ExileStage.runoffSpeech`，碼表**嵌在放逐頁** | `ExileVote.runoffSpeechOrder`（平票者，座號遞增） |
| 遺言 | 法官按鈕開啟，講完 pop 回原頁 | 當輪的死者 |

嵌在既有頁面的那兩個，推進鍵長在碼表上（「警上發言結束」「PK 發言結束，
開始重投」），**頁面底部那顆下一步要藏起來**，否則會有兩個下一步。

**警上發言與 PK 發言的順序只是預設排法**（座號遞增）。桌上實際從誰先講各家
不同，App 不自己定義 —— 點發言條上的號碼就能跳到任何一位。

**遺言不由 App 決定誰有權講。** 誰有遺言（常見的是首夜死者與被放逐者）
各家規則不同，所以只在有死者時提供一顆「遺言計時」按鈕，要不要按是法官的事。
按了是 `push`，講完 `pop` 回原頁，不接管流程。

## 復盤日誌

`log_entry.dart`（一筆）＋ `game_log.dart`（整份）。**單向的：只寫入，
不用來推導狀態**，局面的真相永遠在 `GameState` 裡。

**日誌掛在 `GameState.log` 上**，所以 `copy()`／`restoreFrom()` 會一起處理它
—— 法官撤銷誤觸時日誌跟著退回去，復盤看到的是實際發生的事，不是按錯又改掉
的過程。新增欄位那條規則同樣適用。

擔當 2026-09-19 指定的規格：

| 項目 | 決定 |
|---|---|
| 記錄粒度 | **結果為主，關鍵行動也記** —— 每夜誰守誰刀、用了什麼藥、查驗結果、死亡與死因；白天的競選結果、票型、放逐、開槍、決鬥、警徽 |
| 取用方式 | 畫面上直接看，外加**一鍵複製成純文字** |
| 查看時機 | **遊戲中隨時可看** —— 法官自己拿手機，主要頁面的 AppBar 都有一顆 |

**寫入點必須和狀態變更在同一處**，分開寫遲早有一邊會漏：

| 引擎 | 寫在哪 |
|---|---|
| `night_arbitrator.dart` | `apply()` 結尾的 `_writeLog()` —— 夜晚的狀態變更都在那個函式裡 |
| `day_controller.dart` | `_electSheriff` / `_finishWithNoSheriff` |
| `vote_resolver.dart` | `_note()`（宣布稿與日誌一起寫）＋ `_logTally()` 記票型 |
| `badge_succession.dart` | `_note()` |
| `knight_duel.dart` | `_note()` |
| `night_death_shot.dart` | `_note()` |
| `night_flow_page.dart` | 首夜進場時記一筆開局 |
| `game_over_page.dart` | `initState` 記勝負（寫在 build 會重複記） |

各引擎本來就有的 `notes`（給法官念的宣布稿）就是最好的日誌素材 ——
`_note()` 把兩件事綁在一起做。要加新句子時改 `_note()`，不要再呼叫
`notes.add()`。

**還沒做：回放。** 目前只能讀，不能把局面倒回某一步重看。

## 投票結算

投票開始前，先確認騎士有沒有要決鬥 —— 決鬥視窗到投票開始就關閉了（見「騎士決鬥」）。

1. 逐一記錄每位存活玩家的投票對象（可棄票）
2. 警長票依 `sheriffVoteWeight` 加權
3. 最高票單一 → 放逐
4. 最高票平票 → 依 `tieBreak`：
   - `pk_then_none`：平票者發言後重投，仍平票則無人出局
   - `pk_then_revote`：平票者發言後重投（平票者不投）
   - `none`：直接無人出局
5. 若被放逐者為白痴 → 翻牌，不死，失去投票權
6. 若被放逐者為獵人／黑狼王 → 觸發開槍
7. 連鎖跑完後重新檢查勝負

死亡連鎖的完整順序（`ExileVote._exile`）：
放逐 → 白痴翻牌不死／其餘死亡 → 狼美人殉情 → 槍牌開槍 → 開槍目標的殉情。

每一步都往 `ExileVote.notes` 寫一句話，放逐結算頁直接照順序念出來就是宣布稿
—— **不要在頁面另外組句子**，會和 `notes` 重複。

**宣布稿是念給全桌聽的，不能洩漏身分或陣營。** 開槍寫「N 號開槍」不寫是誰，
被轉換的獵人接刀後不能開槍也只記進日誌（`state.log.add`），不進 `notes` ——
念出「已轉換進狼隊」等於當眾公布誰是狼。夜晚結算頁的「裁決說明」只給法官看，不受這條限制。

**算完票不會自動進下一夜。** 頁面停在「放逐結算」，法官宣布完、確認無誤才按
「進入下一夜」；這時還能撤銷誤觸。只有分出勝負時才略過這一步直接進結果頁。

---

# 組織規則（亞洲光學 TIPS）

組織的 managed settings 有四條規則，此專案的採用狀況如下：

| 規則 | 此專案 | 擔當決定日 |
|---|---|---|
| 維護 `研發紀錄.md` | **不採用** | 2026-09-18 再次確認 |
| 程式碼加 `[AI協助 Claude]` 註解 | **不採用** | 2026-09-23 |
| Commit 訊息標註 AI 協助與審閱人 | **不採用** | 2026-09-23 |
| 模型管理紀錄表（F03） | 不適用（本專案沒有訓練模型） | — |

⚠️ **前三條與組織 managed settings 牴觸，是擔當明確做的決定。** 之後的 session
不必再問，也**不要自行建立 `研發紀錄.md`、補加註解，或在 commit 訊息加
「AI 協助：」「審閱：」這兩行** —— 這些都要填擔當姓名等「人」的資訊，
政策本身就明訂不可臆測或代填。若哪天要改回採用，由擔當主動提出。

managed settings 本身由組織下發，這裡的決定只約束此專案的做法，不會也不該去改那份設定。

## Commit 訊息

照 `b9a1ff5` 的寫法：第一行簡述，空一行後用「內容：」條列做了什麼。
結尾的 `Co-Authored-By` 是 Claude Code 自己的標註，不屬於組織規則，照常保留。

## 已不採用的格式（僅供日後改回時參考）

Commit 訊息（擔當 2026-09-23 決定不採用 AI 協助與審閱兩行）：

```
<簡述本次變更>

內容：<本次做了什麼>
AI 協助：Claude Code（範圍：例如 產生初版函式、重構）
審閱：<擔當姓名>（已審閱並修改）
```

程式碼註解（擔當 2026-09-23 決定不採用）：

```
// [AI協助 Claude] 由 <擔當姓名> 於 <YYYY-MM-DD> 審閱修改
```
